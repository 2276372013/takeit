<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.take.takeDemo.Dao.ArtistDao">

    <!--返回集 -->
    <resultMap id="backMap"
               type="com.take.takeDemo.Entity.Artist">
    </resultMap>

    <!--检索 -->
    <select id="findAll" resultMap="backMap">
        select * from Artist
    </select>

    <!--注册-->
    <insert id="insertArtist" parameterType="Artist">
        insert into Artist values (#{artistname},#{artistinformation},#{Careerexperience},#{Honorrecord},#{actingexperience})
    </insert>

</mapper>